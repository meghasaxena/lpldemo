package LPL.Teacher;

import org.testng.annotations.Test;

//import Com.LPL.Library.Genric;
//import Com.LPL.Library.SuperTestNG;
//import Com.LPL.Library.projectSpecific;

public class AT_Login_Valid
{
  @Test
  public void testAT_Login_Valid()
  {
	 /*
	  //projectSpecific.login("admin","manager");
	  String xlPath="./testdata/AT_Login_Valid.xlsx";
	  String sheetName="Sheet1";
	  int rc = Genric.getXLRowCount(xlPath, sheetName);
	  for(int i=1;i<=rc;i++)
	  {
		  String un=Genric.getXLCellValue(xlPath, sheetName, 0,1);
		  String pwd=Genric.getXLCellValue(xlPath, sheetName, 1,1);
		  
		  projectSpecific.login(un, pwd);
		  projectSpecific.logout();
	}
	
	 */
  }
}
